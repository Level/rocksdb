{
    "version": "0.2.0",
    "configurations": [{
       "type": "lldb",
       "request": "launch",
       "name": "Launch Program",
       "preLaunchTask": "npm: rebuild:dev",
       "program": "node",
       "args": [
          "${workspaceFolder}/${relativeFile}"
       ]
    }]
  }